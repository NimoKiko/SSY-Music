// 获取轮播图接口返回类型
interface bannerResponse {
  banners: Object;
  code: number;
}
// 获取轮播图参数类型
interface bannerParams {
  type: number;
}
// 获取推荐歌单参数类型
interface recommendSongListParams{
  limit: number
}
// 获取推荐歌单返回值类型
interface recommendSongListResponse{
  
}
